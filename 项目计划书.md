# 浏览器翻译插件 — 项目计划书

> **版本:** v1.0  
> **日期:** 2026-06-02  
> **状态:** 规划阶段

---

## 一、项目概述

### 1.1 项目名称

**TransFlow**（暂定名）— 跨平台浏览器多引擎翻译插件

### 1.2 项目定位

一款支持**多翻译引擎可插拔切换**的浏览器扩展，核心功能覆盖整页翻译、划词翻译、双语对照，首版即支持 Chrome / Firefox / Edge / Safari 四大浏览器。

### 1.3 目标用户

- 需要频繁阅读英文/外文网站的中国用户
- 有语言学习需求（通过双语对照辅助学习）
- 对翻译质量有要求，愿意使用付费 API 获得更好结果
- 注重隐私，希望自由选择本地或远程翻译引擎

### 1.4 核心价值

| 痛点 | 解决方案 |
|------|---------|
| Chrome 自带翻译质量一般 | 接入 Claude/GPT/DeepSeek/DeepL 等高质量引擎 |
| 单一翻译引擎不稳定 | 多引擎自动故障切换 |
| 双语学习无原生支持 | 内置双语对照模式（行间穿插 + 一键显示/隐藏） |
| 跨浏览器翻译体验不一致 | WXT 框架一次开发、多浏览器适配 |
| API 调用成本不可控 | 智能缓存减少重复翻译，预估节省 60%+ |

---

## 二、技术选型

### 2.1 总览

| 维度 | 选择 | 理由 |
|------|------|------|
| **开发框架** | WXT | 原生支持多浏览器 Manifest V3，HMR 热更新 |
| **前端框架** | React 18 + TypeScript | 生态丰富，适合中大型设置页和弹窗 |
| **样式方案** | Tailwind CSS + CSS Modules | 快速开发，体积可控 |
| **状态管理** | Zustand | 轻量（<1KB），满足插件级状态需求 |
| **构建工具** | Vite (WXT 内置) | 极速 HMR |
| **包管理** | pnpm | 节省磁盘空间，严格依赖 |
| **测试** | Vitest + Playwright | 单元 + E2E 覆盖 |

### 2.2 浏览器兼容策略

**战略定位：Firefox 是突破口，不是事后补丁。**

绝大多数翻译插件（沉浸式翻译、彩云小译等）都是 Chrome 优先，Firefox 功能滞后数月甚至数年。我们反其道而行——从第 1 天起双浏览器并行开发，以「Firefox 上最好的翻译插件」建立口碑。

**差异点与对策：**

| 差异点 | Chrome | Firefox | 我们的对策 |
|--------|--------|---------|-----------|
| `chrome.storage.session` | ✅ MV3 原生 | ⚠️ Firefox 128+ 才有 | 运行时检测，不支持时降级为 `chrome.storage.local` + 前缀隔离 |
| Service Worker `fetch()` | 无 CSP 限制 | 受限更严格 | 翻译请求全部走 SW → engine，符合我们的架构；额外声明 `host_permissions` |
| `sidePanel` API | ✅ | ❌ 不支持 | Popup 作为通用入口（两边都有），sidePanel 仅在 Chrome 上作为增强 |
| MAIN world 注入 | `userScripts` API | 使用 WXT 的 `world: 'MAIN'` 配置 | WXT 已跨浏览器处理，手动测试验证 |
| 商店审核 | 1-3 天 | 1-2 周（首次审核更严） | 两平台尽早提交首个可审核版本，提前暴露合规问题 |

**并行开发纪律：**

```
开发流程:
  实现新功能 → Chrome 上验证通过 → 立即在 Firefox 上跑冒烟测试
                                    │
                                    ├── 通过 → 提交
                                    └── 失败 → 当天修复，不累积差异

每周 Checklist:
  [ ] Chrome 全功能冒烟测试
  [ ] Firefox 全功能冒烟测试
  [ ] 两浏览器行为差异记录到 docs/browser-diff.md
  [ ] 新增 chrome.* API 调用检查 Firefox 兼容性表
```

**浏览器最低版本：**

| 浏览器 | 最低版本 | Manifest 版本 |
|--------|---------|--------------|
| Chrome | v88+ | MV3 |
| Firefox | v128+ | MV3（`storage.session` 支持起点） |
| Edge | v88+ | MV3 |
| Safari | v15.4+ | MV3 (部分) — Phase 2 |

> **Firefox v128 起点的理由：** `chrome.storage.session` 是我们三层缓存的关键组件。Firefox 128（2024年7月发布）开始支持。低于此版本的 Firefox 用户（估计 < 5%）可降级使用——缓存 L2 层退回到 `chrome.storage.local` 隔离分区，功能完整但 SW 重启后缓存重建稍慢（~200ms vs ~50ms），用户无感知。

**开发速度代价：** 双浏览器并行开发会让初期速度慢约 20%（每次功能要双浏览器验证 + 修复差异）。但 Phase 3 的「Firefox 适配 3 天 → 实测 2 周」变成「Firefox 适配归零」，总体项目周期不增加。

### 2.3 翻译引擎支持矩阵

| 引擎 | 需要 API Key | 语言数量 | 质量等级 | 首版支持 |
|------|:---:|:---:|:---:|:---:|
| **DeepSeek** 💰 | ✅ | 100+ | ⭐⭐⭐⭐⭐ | ✅ |
| **Claude API** (Opus 4.8) | ✅ | 100+ | ⭐⭐⭐⭐⭐ | ✅ |
| **OpenAI GPT-4o** | ✅ | 100+ | ⭐⭐⭐⭐⭐ | ✅ |
| **Google Translate** (免费接口) | ❌ | 130+ | ⭐⭐⭐ | ✅ |
| **DeepL** | ✅ (有免费额度) | 30+ | ⭐⭐⭐⭐ | ✅ |
| **LibreTranslate** (自部署) | ❌ (自部署) | 30+ | ⭐⭐ | Phase 2 |

> 💰 **DeepSeek**：国产大模型，API 兼容 OpenAI 格式，中文翻译质量极高，价格仅为 GPT-4o 的 1/10~1/20。推荐作为主力中文翻译引擎。
>
> **DeepSeek 接入优势：** 由于 DeepSeek API 与 OpenAI API 完全兼容（`baseURL: https://api.deepseek.com/v1`），实现时 `deepseek.ts` 可直接继承 `openai.ts` 的核心逻辑，仅需替换 baseURL 和模型名（`deepseek-chat`），开发成本极低。

用户可设置**主力引擎** + **备用引擎**，调用失败时自动降级切换。

### 2.4 支持的语言清单

插件 UI 层定义**统一语言集合**，各引擎内部映射到自己的语言代码。用户选语言时，引擎路由器自动排除不支持该语言对的引擎。

**核心语言（Phase 1 硬需求）：**

| 语言 | ISO 639-1 | Google | DeepSeek | Claude | OpenAI | DeepL | 中国用户常见场景 |
|------|:---:|:---:|:---:|:---:|:---:|:---:|------|
| 英语 | en | ✅ | ✅ | ✅ | ✅ | ✅ | 英文网页 → 中文 |
| 中文（简体） | zh-CN | ✅ | ✅ | ✅ | ✅ | ✅ | 目标语言 |
| 中文（繁体） | zh-TW | ✅ | ✅ | ✅ | ✅ | ❌ | 繁体网页/港台内容 |
| 日文 | ja | ✅ | ✅ | ✅ | ✅ | ✅ | 日文网页/漫画/游戏 |
| 韩文 | ko | ✅ | ✅ | ✅ | ✅ | ✅ | 韩文网页/韩剧字幕 |
| 法文 | fr | ✅ | ✅ | ✅ | ✅ | ✅ | 学术文献 |
| 德文 | de | ✅ | ✅ | ✅ | ✅ | ✅ | 学术文献/技术文档 |
| 西班牙文 | es | ✅ | ✅ | ✅ | ✅ | ✅ | 拉美内容 |
| 俄文 | ru | ✅ | ✅ | ✅ | ✅ | ✅ | 俄语内容 |
| 葡萄牙文 | pt | ✅ | ✅ | ✅ | ✅ | ✅ | 巴西/葡萄牙内容 |
| 意大利文 | it | ✅ | ✅ | ✅ | ✅ | ✅ | |
| 阿拉伯文 | ar | ✅ | ✅ | ✅ | ✅ | ❌ | |
| 泰文 | th | ✅ | ✅ | ✅ | ✅ | ❌ | 泰国旅游/内容 |
| 越南文 | vi | ✅ | ✅ | ✅ | ✅ | ❌ | |
| 印尼文 | id | ✅ | ✅ | ✅ | ✅ | ❌ | |

**「自动检测」语言：**
- `auto`：由引擎自动检测源语言（所有引擎均支持）
- 这是源语言的默认选项

**源语言 vs 目标语言的非对称性：**
- **源语言列表** = `auto` + 上表全部（用户浏览任意语言页面都可翻译）
- **目标语言默认** = `zh-CN`（中文用户，插件安装即用）
- 用户可在弹窗中一键切换常用目标语言（中/英/日 三个快捷按钮）

**语言引擎匹配逻辑：**

```typescript
// 引擎路由器自动排除不支持该语言对的引擎
function getCapableEngines(sourceLang: string, targetLang: string): TranslationEngine[] {
  return registeredEngines.filter(e => {
    if (sourceLang === 'auto') return e.supportsLanguage(targetLang);
    return e.supportsLanguage(sourceLang) && e.supportsLanguage(targetLang);
  });
}
// 示例：
//   英 → 中：全部 5 个引擎可用
//   英 → 繁体中文：Google/DeepSeek/Claude/OpenAI（DeepL 不支持 zh-TW，自动排除）
//   阿拉伯文 → 英文：Google/DeepSeek/Claude/OpenAI（DeepL 不支持 ar，自动排除）
```

**扩展语言（Phase 2，从 Google 130+ 语言中按需开放）：**

Phase 1 上表 15 种语言覆盖 95% 的使用场景。Phase 2 开放 Google 的完整 130+ 语言列表以及用户自定义语言代码，面向小众语言需求用户。

---

## 三、功能规格

### 3.1 核心功能

#### F1 · 整页翻译

```
场景：用户访问英文网页 → 点击工具栏图标或右键菜单"翻译此页"
     → 全页文本替换为目标语言
```

**TreeWalker 跳过清单（穷举，不写「等」字）：**

| 类别 | 跳过的元素 | 原因 |
|------|-----------|------|
| 代码相关 | `<code>`, `<pre>`, `<kbd>`, `<samp>`, `<var>` | 代码/变量/键盘输入不应翻译 |
| 表单 | `<input>`, `<textarea>` | 翻译会篡改用户输入内容 |
| 媒体 | `<svg>`, `<math>`, `<canvas>` | 无自然语言文本或翻译会破坏渲染 |
| 元数据 | `<script>`, `<style>`, `<noscript>` | 非用户可见内容 |
| 嵌入式 | `<iframe>` | 属于另一个文档上下文，需独立遍历 |
| 已处理 | `[data-translated]` 及其子节点 | 避免重复遍历（但**不作为去重依据**——见下文） |

**SPA 重渲染去重：内容寻址，不靠 DOM 标记。**

Vue/React 在状态变化时会删除整个 DOM 子树并重建——`data-translated` 属性随旧节点一起蒸发。但缓存层里仍然有这笔翻译。新渲染的节点文本和之前完全一样，如果只靠 DOM 标记去重，会再次触发 API 调用。

```
翻译流程：
  提取文本 → hash(文本 + 源语言 + 目标语言) → 查 L1 Map
                                               │
                                    ┌──────────┴──────────┐
                                    │ 命中 → 直接用缓存译文 │
                                    │ 未命中 → 调 API      │
                                    └─────────────────────┘
                                               │
                                    API 返回后写缓存 + 替换 DOM
                                               │
                                    给替换后的节点加上 data-translated
                                    （此时它只是渲染状态标记，不是去重依据）
```

**`data-translated` 的新角色：** 从「去重依据」降级为「渲染遍历优化标记」——TreeWalker 遍历时跳过已翻译节点可以加快遍历速度。去重的唯一权威来源是 L1 Map（内容 hash）。

- 增量翻译：`MutationObserver` 监听动态内容（SPA），新增节点触发增量翻译
- 支持一键还原原文（通过存储的原文→译文映射反转）
- 页面滚动延迟翻译：只翻译视口内 + 预加载缓冲区，减少初始 API 调用

#### F2 · 划词翻译

```
场景：用户选中一段文字 → 弹出翻译气泡 → 显示译文
     → 可复制译文 / 朗读 / 替换原文
```

- 监听 `mouseup` → 检测选区 → 300ms 防抖
- 翻译气泡：绝对定位，智能避让视口边缘
- 气泡功能按钮：复制、朗读（Web Speech API）、翻译为其他语言
- 支持翻译整句（右键菜单"翻译选中文字"）

#### F3 · 双语对照

```
场景：语言学习者阅读英文文章
     → 原文和译文同时显示
     → 可切换行间穿插布局，一键显示/隐藏译文
```

**模式 A：行间穿插（默认）—— 内部包裹策略**

放弃"在原文元素后插入新兄弟节点"的方案（会破坏 `p + p`、`:nth-child()`、Grid/Flex 布局），改用**内部包裹**：

```
插入前:
  <p>Hello, how are you?</p>

插入后:
  <p>
    <span class="tf-original">Hello, how are you?</span>
    <br>
    <span class="tf-translation" aria-hidden="true">你好，你怎么样？</span>
  </p>
```

**为什么内部包裹优于兄弟插入：**

| 问题 | 兄弟插入 `<p>...+<p class="tf">...</p>` | 内部包裹 `<p><span>...</span></p>` |
|------|:---:|:---:|
| 破坏 `p + p` 选择器 | ❌ 译文 `<p>` 成为新的相邻兄弟 | ✅ 不改变兄弟关系 |
| 破坏 `:nth-child()` | ❌ 新增兄弟改变索引 | ✅ 子元素变化不影响父级索引 |
| 破坏 Grid/Flex 布局 | ❌ 译文节点成为新的 grid/flex item | ✅ `display:contents` 可进一步透明化 |
| 屏幕阅读器读译文 | ⚠️ 需 aria-hidden | ✅ `aria-hidden="true"` 彻底隐藏 |

**三种包裹策略的逐级降级：**

```typescript
// lib/bilingual-renderer.ts

type InsertionStrategy = 'wrap-text' | 'sibling-fallback' | 'sidebar-fallback';

function determineStrategy(element: Element): InsertionStrategy {
  // 1. 大部分元素：直接包裹文本节点 → 'wrap-text'
  // 2. 特殊元素（<input>, <textarea>, <select>）→ 'sibling-fallback'
  //    这些元素不能包含子节点，只能在兄弟位置插入伪元素覆盖层
  // 3. 复杂布局容器（CSS Grid/Flex 且高度依赖子元素顺序）→ 'sidebar-fallback'
  //    检测：getComputedStyle(el).display === 'grid' | 'flex'
  //          且 el.children.length > 阈值
}
```

**无障碍（a11y）硬约束：**

```
所有译文 DOM 节点必须设置:
  aria-hidden="true"      ← 从无障碍树中移除
  role="presentation"     ← 消除语义角色（冗余但安全）
  tabindex="-1"           ← 不可聚焦
  user-select: none       ← 可选：防止翻译文本被选中后二次翻译

屏幕阅读器输出:
  替换前: "Hello comma how are you question mark"
  替换后: "Hello comma how are you question mark" （译文被完全跳过）
  ✅ 盲人用户听到的仍然是原文（按页面语言朗读）
```

**布局保护：**

```css
/* 所有译文相关元素 */
.tf-translation {
  display: block;           /* 默认：译文另起一行，不影响行内布局 */
  color: var(--tf-translation-color, #666);
  font-size: var(--tf-translation-size, 0.9em);
  font-style: italic;
  user-select: none;
}

/* 当父元素是 flex/grid 时，用 display:contents 透明化包裹 */
.tf-original,
.tf-translation {
  /* 不影响父级 flex/grid 的计算 */
}

/* 对于 flex/grid 父级，整个包裹组使用 display:contents */
.tf-bilingual-wrapper {
  display: contents;        /* CSS 布局视为此元素不存在 */
}
```

**模式 B：侧栏对照**

```
┌──────────────────────┬──────────────────────┐
│  原文                 │  译文                 │
│                      │                      │
│  Hello, how are you? │  你好，你怎么样？      │
│  I'm fine, thank you.│  我很好，谢谢。        │
└──────────────────────┴──────────────────────┘
```

- 复制页面内容到 Shadow DOM 容器，完全隔离样式
- 左右分栏，同步滚动
- 适合长篇阅读
- 当行间穿插检测到复杂布局时，自动建议切换到侧栏模式

**研发文档约束（写入 CONTRIBUTING.md）：**

> ⚠️ 行间穿插模式不保证在所有网站上完美渲染。已知风险：通过伪元素 `::before`/`::after` 装饰段落的网站可能出现视觉重叠。遇到此类问题时，请引导用户切换到侧栏模式，或提交 issue 附上 URL 以便我们针对性适配。

### 3.2 增强功能（Phase 2）

| 功能 | 描述 |
|------|------|
| **自动翻译** | 检测到非母语页面 → 自动翻译（可配置黑名单） |
| **翻译历史** | 记录翻译历史，支持搜索、导出 |
| **快捷键** | 全局快捷键切换翻译状态 |
| **PDF 翻译** | 内嵌 PDF 查看器的文本翻译 |
| **字幕翻译** | 视频内嵌字幕实时翻译 |
| **自定义术语表** | 用户自定义翻译替换规则 |
| **多语言 TTS** | 高质量语音朗读 |

---

## 四、架构设计

### 4.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    Browser Extension                     │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────────┐ │
│  │   Popup UI   │  │  Options UI  │  │ Popup UI      │ │
│  │  (React)     │  │  (React)     │  │ (React)       │ │
│  └──────┬───────┘  └──────┬───────┘  └───────┬───────┘ │
│         │                 │                  │          │
│         └─────────┬───────┴──────────────────┘          │
│                   │                                     │
│         chrome.runtime.sendMessage                      │
│                   │                                     │
│  ┌────────────────┴────────────────────────────────┐   │
│  │         Background Service Worker                │   │
│  │                                                  │   │
│  │  ┌──────────┐ ┌──────────┐ ┌─────────────────┐  │   │
│  │  │  Cache   │ │  Engine  │ │  Storage Mgr    │  │   │
│  │  │  Layer   │ │  Router  │ │  (settings/keys)│  │   │
│  │  └────┬─────┘ └────┬─────┘ └────────┬────────┘  │   │
│  │       │            │              │             │   │
│  │       └────────────┴──────────────┘             │   │
│  │                    │                             │   │
│  │           ┌────────┴────────┐                    │   │
│  │           │  Engine Adapter │                    │   │
│  │           │  ───────────────│                    │   │
│  │           │  · DeepSeek     │                    │   │
│  │           │  · Claude       │                    │   │
│  │           │  · OpenAI       │                    │   │
│  │           │  · Google       │                    │   │
│  │           │  · DeepL        │                    │   │
│  │           └────────┬────────┘                    │   │
│  └────────────────────┼────────────────────────────┘   │
│                       │                                │
└───────────────────────┼────────────────────────────────┘
                        │
                  ┌─────┴─────┐
                  │  External │
                  │  APIs     │
                  └───────────┘

┌─────────────────────────────────────────────────────────┐
│                  Content Script                          │
│                                                          │
│  ┌────────────┐  ┌──────────────┐  ┌─────────────────┐ │
│  │ DOM Walker │  │  Selection   │  │  Bilingual      │ │
│  │ (TreeWalker│  │  Detector    │  │  Renderer       │ │
│  │  +Mutation │  │  + Tooltip   │  │  (行间+侧栏)    │ │
│  │  Observer) │  │              │  │                 │ │
│  └─────┬──────┘  └──────┬───────┘  └────────┬────────┘ │
│        │               │                   │           │
│        └───────────────┼───────────────────┘           │
│                        │                               │
│               Message Passing                          │
│               (chrome.runtime.sendMessage)             │
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
              Background Service Worker
```

### 4.2 目录结构

```
translationPro/
├── src/
│   ├── entrypoints/
│   │   ├── background.ts            # Service Worker 主入口（唯一持有 API Key 的模块）
│   │   ├── content.ts               # ISOLATED world Content Script（默认路径）
│   │   ├── content-main.ts          # MAIN world Content Script（CSP 降级路径，仅 postMessage 桥接）
│   │   ├── popup/                   # 工具栏弹出窗口
│   │   │   ├── index.html
│   │   │   ├── main.tsx
│   │   │   ├── App.tsx
│   │   │   ├── components/
│   │   │   │   ├── LanguageSelector.tsx
│   │   │   │   ├── EngineStatus.tsx
│   │   │   │   ├── ToneSelector.tsx        # Phase 2: 翻译风格选择
│   │   │   │   └── QuickActions.tsx
│   │   │   └── styles.module.css
│   │   └── options/                 # 设置页面
│   │       ├── index.html
│   │       ├── main.tsx
│   │       ├── App.tsx
│   │       ├── components/
│   │       │   ├── EngineConfig.tsx
│   │       │   ├── ApiKeyManager.tsx        # API Key 管理 + 透明安全说明
│   │       │   ├── LanguageSettings.tsx
│   │       │   ├── PromptTemplateEditor.tsx # Phase 2: 自定义 prompt 模板
│   │       │   └── AppearanceSettings.tsx
│   │       └── styles.module.css
│   │
│   ├── engines/                     # 翻译引擎抽象层（仅被 background.ts import）
│   │   ├── base.ts                  # AIEngine 抽象类 + buildSystemPrompt()
│   │   ├── deepseek.ts              # DeepSeek（继承 OpenAI 逻辑，替换 baseURL）
│   │   ├── claude.ts
│   │   ├── openai.ts
│   │   ├── google.ts                # 免费引擎：解析 Google Translate 接口
│   │   ├── deepl.ts
│   │   └── index.ts                 # 引擎注册表 & 路由 & 故障切换
│   │
│   ├── lib/                         # 核心库
│   │   ├── dom-walker.ts            # TreeWalker 文本提取 + SKIP_TAGS 穷举清单 + TextFragment 元数据采集 + hash 去重
│   │   ├── bilingual-renderer.ts    # 双语对照：内部包裹 / 兄弟降级 / 侧栏降级
│   │   ├── strategy-inferrer.ts     # 翻译策略推断器：inferStrategy(fragments, ctx)
│   │   ├── selection-handler.ts     # 划词检测 & 翻译气泡
│   │   ├── cache.ts                 # 三层缓存：Map → session → local + 容量守卫
│   │   ├── storage.ts               # chrome.storage 封装
│   │   ├── message-bus.ts           # 消息通信 + ISOLATED↔MAIN world 桥接
│   │   ├── types.ts                 # 全局类型：TextFragment, PageContext, ApiKey (opaque), TranslationStrategy, ...
│   │   ├── security.ts              # ApiKey 类型守卫 + 日志过滤（防止 Key 泄露到 console）
│   │   └── constants.ts             # 常量 & 默认配置
│   │
│   ├── components/                  # 共享 UI 组件
│   │   ├── TranslateButton.tsx
│   │   ├── Toast.tsx
│   │   └── LoadingSpinner.tsx
│   │
│   └── styles/
│       └── globals.css
│       └── bilingual.css            # 双语对照样式（含 display:contents 保护）
│
├── public/
│   └── icons/
│       ├── icon-16.png
│       ├── icon-48.png
│       └── icon-128.png
│
├── tests/
│   ├── unit/
│   │   ├── cache.test.ts
│   │   ├── dom-walker.test.ts
│   │   ├── strategy-inferrer.test.ts
│   │   ├── engines.test.ts
│   │   └── bilingual-renderer.test.ts
│   └── e2e/
│       ├── full-page.spec.ts
│       └── selection.spec.ts
│
├── wxt.config.ts                    # WXT 配置（含 ISOLATED/MAIN world 注入规则）
├── package.json
├── tsconfig.json
├── tailwind.config.ts
├── vitest.config.ts
├── eslint.config.js                 # 含 no-restricted-imports/no-restricted-globals
├── CONTRIBUTING.md                  # PR Checklist + 安全约束文档
├── README.md
└── 项目计划书.md                    # 本文档
```

**模块依赖约束（目录级强制）：**

```
engines/          ← 只被 background.ts import
lib/types.ts      ← 被所有模块 import（仅类型，无运行时）
lib/security.ts   ← 被 background.ts import（ApiKey 类型守卫）
lib/cache.ts      ← 被 background.ts import
lib/*             ← 被 content.ts, content-main.ts import（工具函数，不涉及 Key）

content.ts        ← 不 import engines/，不 import background.ts
content-main.ts   ← 不 import 任何含 Key 的模块，仅做 postMessage 转发
```

**ESLint 强制规则（eslint.config.js 关键片段）：**

```javascript
// content.ts 和 content-main.ts 中禁止的 import 和全局 API
overrides: [
  {
    files: ['src/entrypoints/content.ts', 'src/entrypoints/content-main.ts'],
    rules: {
      'no-restricted-globals': ['error', { name: 'fetch', message: 'Content Script 禁止直接 fetch()，请通过 sendMessage 转发到 SW' }],
      'no-restricted-imports': ['error', {
        patterns: [{ group: ['../engines/**', '../lib/cache', '../lib/storage'], message: 'Content Script 禁止直接访问引擎/缓存/存储模块' }]
      }]
    }
  }
]
```

### 4.3 核心接口设计

#### 4.3.1 翻译策略：两个维度的控制

同一个 `TranslateRequest`，在不同场景下需要不同的翻译行为。翻译 "Submit" 按钮和翻译一段学术论文的策略不可能一样。我们通过**两个维度**来区分：

```
维度 1: 自动上下文（Content Script 自动采集，零用户配置）
  ├── HTML 标签类型（<button> vs <p> vs <h1>）
  ├── 页面元数据（<html lang>、meta description、OG tags）
  └── 文本特征（长度、是否含代码、是否 placeholder）

维度 2: 翻译风格（Phase 1 自动推断 → Phase 2 用户可覆盖）
  ├── technical（技术文档：保留变量名、不翻译代码块）
  ├── literary  （文学内容：追求文采和语气传达）
  ├── casual    （日常对话/社交媒体：口语化）
  └── precise   （UI 短文本/按钮/菜单：精确、简短）
```

**Phase 1：** 维度 1 全自动，维度 2 从页面上下文自动推断（比如 URL 含 `/docs/` → technical），用户零感知。  
**Phase 2：** 开放 per-engine 自定义 prompt 模板，用户可以写 `"将以下{tagName}文本翻译为{targetLang}，保留所有换行和缩进：{text}"`，完全控制翻译行为。

#### 4.3.2 接口定义

```typescript
// ---------- Content Script → 文本提取的元数据 ----------

interface TextFragment {
  text: string;
  meta: {
    tagName: string;          // 'P' | 'BUTTON' | 'H1' | 'A' | 'SPAN' | 'INPUT' | ...
    isPlaceholder: boolean;   // input/textarea placeholder?
    isLink: boolean;
    ancestorChain: string;    // 'BODY > DIV > ARTICLE > P'，辅助判断上下文
    textLength: number;
    containsCode: boolean;    // 检测到代码片段？
  };
}

// ---------- 页面级上下文（Content Script 自动采集）----------

interface PageContext {
  url: string;
  title: string;
  metaDescription?: string;
  htmlLang?: string;          // <html lang="en">
  ogLocale?: string;          // OG:locale meta
  inferredDomain?: string;    // 从 URL/内容推断：'tech' | 'academic' | 'general'
}

// ---------- 翻译策略 ----------

type ToneHint = 'auto' | 'technical' | 'literary' | 'casual' | 'precise';

interface TranslationStrategy {
  tone: ToneHint;             // Phase 1: 自动推断; Phase 2: 用户可选择
  preserveFormatting: boolean; // 保留 HTML/markdown 空白与换行
  glossary?: Record<string, string>;  // 术语表（Phase 2）
}

// ---------- 翻译请求 ----------

interface TranslateRequest {
  fragments: TextFragment[];  // 替换原来的 texts: string[]，携带元数据
  sourceLang: string;         // "auto" 表示自动检测
  targetLang: string;
  pageContext: PageContext;   // 自动采集，非用户配置
  strategy: TranslationStrategy;
}

// ---------- 翻译结果 ----------

interface TranslateResult {
  translations: string[];     // 与 fragments 一一对应
  engine: string;
  sourceLangDetected?: string;
  fromCache: boolean;
  latencyMs: number;
  strategyUsed: TranslationStrategy;  // 记录实际使用的策略，便于调试
}

// ---------- 引擎接口 ----------

interface TranslationEngine {
  readonly id: string;
  readonly name: string;
  readonly requiresApiKey: boolean;
  readonly supportedLanguages: string[];
  translate(req: TranslateRequest): Promise<TranslateResult>;
  validateApiKey(key: string): Promise<boolean>;
}

// ---------- AI 引擎基类：Prompt 组装 ----------

abstract class AIEngine implements TranslationEngine {
  // Phase 1: 硬编码的通用 prompt（够好但不完美）
  protected buildSystemPrompt(strategy: TranslationStrategy, ctx: PageContext): string {
    // 由子类覆盖，或使用默认实现
  }

  // Phase 2: 用户自定义 prompt 模板（会覆盖 buildSystemPrompt）
  protected resolveUserTemplate(
    template: string | undefined,
    fragment: TextFragment,
    ctx: PageContext
  ): string {
    // 变量替换：{text} {tagName} {targetLang} {pageDomain} ...
  }
}

// ---------- 策略推断器（Phase 1 核心）----------

function inferStrategy(fragments: TextFragment[], ctx: PageContext): TranslationStrategy {
  // 规则驱动，零配置：
  //   1. 所有 fragment 都是 BUTTON/INPUT/A/SPAN + 短文本 → tone: 'precise'
  //   2. URL 含 /docs/ 或 /documentation/ → tone: 'technical'
  //   3. fragment 含 <code>/<pre> → preserveFormatting: true
  //   4. 默认 → tone: 'auto'（交给引擎自己的判断）
}

// ---------- 缓存接口 ----------

interface CacheEntry {
  key: string;              // hash(原文 + sourceLang + targetLang + engineId)
  original: string;
  translation: string;
  engine: string;
  timestamp: number;
  hitCount: number;
}

// ---------- 消息协议 ----------

type MessageType =
  | { type: 'TRANSLATE_FRAGMENTS'; payload: TranslateRequest }
  | { type: 'TRANSLATE_SELECTION'; payload: { text: string; targetLang: string } }
  | { type: 'TRANSLATE_PAGE'; payload: { tabId: number; targetLang: string; strategy?: Partial<TranslationStrategy> } }
  | { type: 'GET_ENGINES' }
  | { type: 'SET_ENGINE'; payload: { engineId: string } }
  | { type: 'TOGGLE_BILINGUAL'; payload: { enabled: boolean; mode: 'inline' | 'sidebar' } }
  | { type: 'REVERT_PAGE'; payload: { tabId: number } }
  | { type: 'UPDATE_PROMPT_TEMPLATE'; payload: { engineId: string; template: string } };  // Phase 2
```

#### 4.3.3 Prompt 组装流程（以 DeepSeek 为例）

```
Content Script 提取:
  fragment = {
    text: "Click here to submit",
    meta: { tagName: 'BUTTON', isPlaceholder: false, ancestorChain: '...', ... }
  }
  pageContext = {
    url: "https://example.com/docs/api-reference",
    title: "API Reference - Example",
    htmlLang: "en",
    inferredDomain: "tech"
  }
        │
        ▼
inferStrategy() 自动判断:
  → tone: 'precise'  (因为 tagName 是 BUTTON + 短文本)
  → preserveFormatting: false
        │
        ▼
DeepSeekEngine.buildSystemPrompt():
  → "You are a translator. Translate short UI strings precisely.
     Preserve the action-oriented tone of buttons.
     Never translate technical terms that are conventionally kept in English.
     Return JSON: [{translation}]"
        │
        ▼
API 请求 body:
  {
    model: "deepseek-chat",
    messages: [
      { role: "system", content: "<system prompt>" },
      { role: "user", content: "Translate to zh-CN:\n[0] Click here to submit" }
    ]
  }
```

#### 4.3.4 Phase 1 vs Phase 2 的分界线

| 能力 | Phase 1 | Phase 2 |
|------|---------|---------|
| `inferStrategy()` 自动推断 | ✅ 硬编码规则 | ✅ 规则可配置 |
| `buildSystemPrompt()` | ✅ 每个引擎一个硬编码版本 | ✅ 用户在设置页自定义模板 |
| 术语表 (`glossary`) | ❌ | ✅ 用户可导入/编辑 |
| `ToneHint` 用户选择 | ❌（纯自动） | ✅ 弹出窗口可选 |
| 自定义变量（`{tagName}` 等） | ❌ | ✅ 模板变量系统 |

> **设计原则：** Phase 1 做到「开箱即用，零配置，AI 引擎翻译质量明显优于传统翻译」，Phase 2 做到「控制欲用户可以对每个引擎的 prompt 进行精确调校」。

### 4.4 缓存策略

#### 三层架构

```
写入路径:  Map ──(write-through + 容量守卫)──→ session ──(LRU 淘汰)──→ local
读取路径:  Map (同步, <1ms)  ←──(SW 重启时一次性重建)──  session
```

| 层级 | 存储介质 | 容量 | 生命周期 | 读延迟 | 用途 |
|------|---------|------|---------|--------|------|
| L1 | 内存 Map | ~5000 条 | SW 存活期间 | < 1ms | 热点读取，批量查缓存 |
| L2 | `chrome.storage.session` | 10MB 总配额 | 浏览器重启 | ~1-5ms | SW 被杀后重建 L1 |
| L3 | `chrome.storage.local` | 10MB | 持久（30 天过期） | ~5-10ms | 跨会话持久化 |

#### MV3 硬限制与对策

> **问题：** Service Worker 空闲 ~30 秒后被浏览器终止，L1 Map 直接归零。

**解决：** L1 不再依赖"页面一直开着"的假设。

- **写入路径 (write-through)：** 每次写入 Map 的同时，异步写入 session storage。SW 被杀后重启，从 session 一次性反序列化重建 Map（~几十 ms），用户无感知。
- **容量守卫：** `chrome.storage.session` 的 10MB 是整个 storage 的总配额，不是按 key 粒度。每次 write-through 前检查 `chrome.storage.session.getBytesInUse()`，若已用 > 8MB（留 20% 缓冲），触发 LRU 淘汰后再写入，防止 `QUOTA_BYTES` 错误。
- **降级写入：** session 接近配额时，LRU 淘汰的条目不直接丢弃，而是进一步降级写入 local storage 做持久化。

#### 容量守卫实现

```typescript
// lib/cache.ts — write-through 路径上的容量守卫

const SESSION_QUOTA = 10 * 1024 * 1024;  // 10MB
const HIGH_WATER = 0.8;                   // 80% 触发淘汰

async function writeThrough(entry: CacheEntry): Promise<void> {
  // 1. 写 L1（同步）
  l1Map.set(entry.key, entry);

  // 2. 写 L2 前检查配额
  const bytesUsed = await chrome.storage.session.getBytesInUse();
  if (bytesUsed !== undefined && bytesUsed > SESSION_QUOTA * HIGH_WATER) {
    await evictLRU(Math.ceil(l1Map.size * 0.3)); // 淘汰 30%
  }

  // 3. 写入（可能仍失败则二次淘汰）
  try {
    await chrome.storage.session.set({ [entry.key]: entry });
  } catch (e) {
    if (e.message?.includes('QUOTA_BYTES')) {
      await evictLRU(Math.ceil(l1Map.size * 0.5)); // 激进淘汰 50%
      await chrome.storage.session.set({ [entry.key]: entry });
    }
  }
}
```

#### SW 启动时重建 L1

```typescript
// background.ts — SW 启动钩子

chrome.runtime.onStartup.addListener(async () => {
  const all = await chrome.storage.session.get(null); // 全量读取
  for (const [key, entry] of Object.entries(all)) {
    l1Map.set(key, entry as CacheEntry);
  }
  console.log(`[Cache] L1 rebuilt: ${l1Map.size} entries from session`);
});
```

#### 说明

- **为什么保留 Map：** 整页翻译可能批量查 200 条缓存。`Map.get()` 是同步 O(1)，200 次 < 1ms。纯 session storage 每次异步 1-5ms，累计延迟 200-1000ms，无法满足"缓存命中 < 10ms"的性能目标。
- **为什么不直接用 IndexedDB 做 L2：** session storage API 更简洁、与 local storage 同 API 族、延迟更低。10MB 对纯文本翻译缓存足够（约 6-7 万条）。
- **L3 (local) 的角色：** session 淘汰的条目写入 local 做长期持久化。重要性低于 L1/L2——主要服务于跨浏览器会话的缓存复用。

**缓存键:** `MD5(原文 + 源语言 + 目标语言 + 引擎ID)`

**预估效果:**
- 每天浏览 50 页，每页 200 条 text node → 10,000 条/天
- 假设命中率 60%（页面重复片段多）→ 每天节省 6,000 次 API 调用
- 缓存 1 万条 ≈ 1.5MB → session 10MB 可缓存约 6-7 万条

### 4.5 数据流

```
用户操作（点击翻译/划词）
        │
        ▼
┌──────────────────┐
│  Content Script   │  提取文本 → 去重
└────────┬─────────┘
         │ chrome.runtime.sendMessage
         ▼
┌──────────────────┐
│ Service Worker   │  接收请求
└────────┬─────────┘
         │
    ┌────┴────┐
    │ 查缓存？ │──YES──→ 返回缓存结果
    └────┬────┘
         │ NO
         ▼
┌──────────────────┐
│  Engine Router   │  选择引擎（主引擎 → 备用 → 失败降级）
└────────┬─────────┘
         │
    ┌────┴────┐
    │ API Key？│──MISSING──→ 降级到免费引擎
    └────┬────┘
         │ OK
         ▼
┌──────────────────┐
│  External API    │  发起翻译请求
└────────┬─────────┘
         │
    ┌────┴────┐
    │ 成功？   │──NO──→ 重试（最多2次）→ 备用引擎
    └────┬────┘
         │ YES
         ▼
┌──────────────────┐
│  写入缓存        │
│  返回结果        │
└────────┬─────────┘
         │
         ▼
 Content Script ← 替换 DOM
```

---

## 五、UI 设计概览

### 5.1 弹出窗口 (Popup)

```
┌──────────────────────────┐
│  🌐 TransFlow            │
│  ────────────────────────│
│                           │
│  目标语言: [中文 ▼]       │
│                           │
│  翻译引擎: [🤖 Claude ▼]  │
│                           │
│  ┌──────────────────────┐ │
│  │  🔄 翻译此页面       │ │
│  └──────────────────────┘ │
│  ┌──────────────────────┐ │
│  │  📖 双语对照（行间） │ │
│  └──────────────────────┘ │
│  ┌──────────────────────┐ │
│  │  ↩ 还原原文         │ │
│  └──────────────────────┘ │
│                           │
│  ────────────────────────│
│  本月节省: 1,234 次调用   │
│  ⚙ 设置                  │
└──────────────────────────┘
```

### 5.2 设置页面 (Options)

```
┌──────────────────────────────────────┐
│  ⚙ 设置                             │
│  ───────────────────────────────────│
│                                      │
│  [引擎] [语言] [外观] [缓存] [关于]  │
│  ───────────────────────────────────│
│                                      │
│  翻译引擎管理                        │
│  ┌─────────────────────────────────┐│
│  │ 🔴 DeepSeek (推荐)     [配置]   ││
│  │    API Key: sk-*** [已设置]      ││
│  │    状态: ✅ 已连接               ││
│  │    优先级: [⭐ 主力引擎]         ││
│  ├─────────────────────────────────┤│
│  │ 🤖 Claude API          [配置]   ││
│  │    API Key: sk-ant-*** [修改]   ││
│  │    状态: ✅ 已连接              ││
│  │    优先级: [备用引擎]           ││
│  ├─────────────────────────────────┤│
│  │ 🔵 Google Translate    [配置]   ││
│  │    状态: ✅ 可用（免费）        ││
│  │    优先级: [备用引擎]           ││
│  ├─────────────────────────────────┤│
│  │ 🟢 DeepL               [配置]   ││
│  │    API Key: [未设置]    [配置]   ││
│  │    状态: ⚠ 未启用               ││
│  │    优先级: [未启用]             ││
│  └─────────────────────────────────┘│
│  [+ 添加引擎]                       │
│                                      │
│  ───────────────────────────────────│
│  默认语言设置                        │
│  源语言: [自动检测 ▼]               │
│  目标语言: [中文（简体）▼]          │
│  第二目标语言: [日文 ▼]             │
│                                      │
│  ───────────────────────────────────│
│  自动翻译                            │
│  [✓] 检测到英文页面时自动翻译       │
│  [ ] 检测到日文页面时自动翻译       │
│  不翻译以下网站:                     │
│  ┌─────────────────────────────────┐│
│  │ github.com, stackoverflow.com   ││
│  └─────────────────────────────────┘│
└──────────────────────────────────────┘
```

### 5.3 划词气泡

```
  用户选中的英文文本 here
        │
        ▼
  ┌───────────────────┐
  │ 你好，你怎么样？   │  ← 译文（主显示）
  │ ─────────────────  │
  │ How are you?       │  ← 原文（可收起）
  │                    │
  │ [📋复制] [🔊朗读]  │
  └───────────────────┘
```

---

## 六、开发计划

### 6.1 Phase 1：核心基础（第 1-4 周）

**目标：项目骨架 + 基础翻译能力，Chrome + Firefox 并行**

> ⚠️ 从第一天起，每个任务完成后都在 Chrome **和** Firefox 上验证。额外开销约 20%，但消除 Phase 3 的「Firefox 适配惊喜」。

| # | 任务 | 预估 | 双浏览器验证点 |
|---|------|------|---------------|
| 1.1 | 项目脚手架搭建（WXT + React + TS） | 2天 | 空白扩展在 Chrome + Firefox 均可加载，`browser`/`chrome` polyfill 可用 |
| 1.2 | 类型系统 & 引擎接口定义 | 1天 | — |
| 1.3 | `chrome.storage` 封装（含 Firefox compat 处理） | 1.5天 | `storage.session` 在 Firefox 128 可用；低于 128 降级到 local + 前缀隔离 |
| 1.4 | Google Translate 引擎实现 | 2天 | SW 中 fetch 在 Firefox 正常返回 |
| 1.5 | DeepSeek API 引擎实现 | 1天 | — |
| 1.6 | Claude API 引擎实现 | 2天 | — |
| 1.7 | OpenAI API 引擎实现 | 1天 | — |
| 1.8 | 引擎路由 & 故障切换 | 1.5天 | 故障切换在 Firefox 上触发正常 |
| 1.9 | 缓存层实现（三层 + 容量守卫） | 2.5天 | session quota 在 Firefox 上行为一致 |
| 1.10 | Popup 基础 UI | 2天 | Popup 在 Firefox 上尺寸/行为正常 |
| 1.11 | 消息通信机制 | 1天 | ISOLATED ↔ MAIN bridge 两浏览器均可工作 |
| 1.12 | 浏览器差异文档初始化 | 0.5天 | `docs/browser-diff.md` 模板 + 首个差异条目 |

**里程碑:** ✅ Chrome + Firefox 均可通过 Popup 触发整页翻译（Google + DeepSeek），设置页可配 API Key。`docs/browser-diff.md` 持续记录两浏览器差异。

### 6.2 Phase 2：核心功能（第 5-8 周）

**目标：三大核心功能完整可用，持续双浏览器验证**

| # | 任务 | 预估 | 双浏览器验证点 |
|---|------|------|---------------|
| 2.1 | DOM Walker (TreeWalker + SKIP_TAGS 穷举 + hash 去重) | 4天 | Content-hash 作为去重权威来源，`data-translated` 仅作为遍历优化标记 |
| 2.2 | MutationObserver 增量翻译 | 2.5天 | SPA 页面（如 React/Vue 渲染）在 Firefox 上增量翻译正常 |
| 2.3 | 整页翻译 & 还原 | 2.5天 | 整页翻译 + 还原按钮两浏览器均可工作 |
| 2.4 | 划词检测 & 翻译气泡 | 3.5天 | 气泡定位算法在 Firefox 上避让视口边缘正确 |
| 2.5 | 双语对照 - 行间穿插模式 | 3.5天 | 内部包裹 DOM 策略在 Firefox 上渲染无差异 |
| 2.6 | Options 设置页面完整版 | 3.5天 | 设置页存储读取两浏览器行为一致 |
| 2.7 | DeepL 引擎实现 | 2天 | — |
| 2.8 | 错误处理 & 用户提示 | 1天 | 错误提示在 Firefox 通知系统中显示正常 |

**里程碑:** ✅ Chrome + Firefox 双平台，三大核心功能（整页/划词/双语）均可用，多引擎可切换。

### 6.3 Phase 3：打磨 & 发布准备（第 9-10 周）

**目标：体验打磨 + 测试 + 商店上架。Firefox 平台已与 Chrome 同步到达可发布状态。**

| # | 任务 | 预估 | 产出 |
|---|------|------|------|
| 3.1 | 双语对照 - 显示/隐藏切换 + 样式打磨 | 1天 | 一键开关译文层，优化视觉体验 |
| 3.2 | 右键菜单集成 | 1天 | 上下文菜单（Chrome + Firefox 菜单 API 有差异，WXT 已统一） |
| 3.3 | 键盘快捷键 | 1天 | 全局快捷操作（Firefox 需验证 `commands` 键位冲突） |
| 3.4 | 缓存管理 UI & 统计 | 2天 | 用户可见的缓存控制 |
| 3.5 | E2E 测试（Playwright — Chrome） | 2天 | Chrome 自动化测试 |
| 3.6 | Firefox 手动回归测试 | 1.5天 | 全功能 checklist 过一遍（因为 Playwright 对 Firefox 扩展支持有限） |
| 3.7 | 性能优化 & 代码审查 | 2天 | 代码质量 |
| 3.8 | Chrome Web Store 上架准备 | 1天 | Store listing、截图、描述文案 |
| 3.9 | Firefox Add-ons 上架准备 | 1天 | AMO listing、首次审核前置自查 |

**里程碑:** ✅ Chrome + Firefox 双平台均提交商店审核。

### 6.4 Phase 4：发布 & 迭代（第 11 周+）

| # | 任务 | 预估 |
|---|------|------|
| 4.1 | Chrome Web Store 上架 | 1周 |
| 4.2 | Firefox Add-ons 上架 | 3天 |
| 4.3 | 用户反馈收集 & 迭代 | 持续 |
| 4.4 | Safari 适配 | 1周 |
| 4.5 | 术语表 & 自定义规则 | 2周 |
| 4.6 | PDF / 字幕翻译 | 2周 |

### 6.5 浏览器外形态（Phase 4+）

> 引擎层和缓存层从一开始就写成零浏览器依赖的纯 JS/TS。浏览器插件发布后，核心逻辑可直接复用到其他平台。

**可复用层 vs 平台适配层：**

```
                      ┌─────────────────────────┐
                      │   @transflow/core        │  ← 纯 JS/TS，可从插件的 engines/ + lib/ 直接抽出
                      │   · 翻译引擎（5个）       │
                      │   · 缓存层               │
                      │   · 策略推断器            │
                      │   · 类型定义              │
                      └───────────┬─────────────┘
                                  │
          ┌───────────────────────┼───────────────────────┐
          │                       │                       │
          ▼                       ▼                       ▼
   ┌────────────┐         ┌────────────┐         ┌────────────┐
   │  CLI 工具  │         │  VS Code   │         │  Raycast   │
   │  npm i -g  │         │  扩展      │         │  插件      │
   │            │         │            │         │            │
   │ 复用率 60% │         │ 复用率 55% │         │ 复用率 50% │
   └────────────┘         └────────────┘         └────────────┘
```

**各形态复用分析：**

| 形态 | 复用核心 | 需新写的平台层 | 场景 |
|------|---------|---------------|------|
| **CLI** | engines/ + cache + strategy-inferrer + types | stdin/stdout 管道、文件读写、配置文件解析（~200行） | `echo "text" \| transflow --to zh`，CI 中批量翻译文档 |
| **VS Code** | 同上 | 选中文本获取、装饰层渲染（~300行） | 编辑器内翻译代码注释/字符串 |
| **Raycast** | 同上 | Raycast 插件协议（~150行） | macOS 全局热键划词翻译 |

**发布策略：**

| 阶段 | 内容 |
|------|------|
| 浏览器插件稳定后 | 从仓库抽出 `engines/` + `lib/` → 独立 npm 包 `@transflow/core` |
| 同期 | `npx @transflow/cli` — 验证 core 包可复用性的第一个客户端 |
| 社区驱动 | VS Code / Raycast 插件开放社区贡献，core 包已就绪 |

**许可与定价：**

所有形态（浏览器插件 / CLI / VS Code / Raycast）**永久免费 + 开源**（MIT License）。软件不收费，翻译费用仅由你自己提供的 API Key 承担（如 DeepSeek API 约 ¥2/百万 token）。这与沉浸式翻译的 ¥69/年订阅形成根本性差异。

> **原则：** 浏览器插件始终是主战场。CLI / VS Code / Raycast 不消耗 Phase 1-3 的任何时间，仅利用 core 包的自然复用。如果社区有人想贡献其他平台——core 包已经在那里了。

### 6.5 甘特图概览

```
Week 1-4:   ████████████████████████  Phase 1: 核心基础（Chrome + Firefox 并行）
Week 5-8:                            ████████████████████████  Phase 2: 核心功能（持续双浏览器验证）
Week 9-10:                                                      ██████████████  Phase 3: 打磨 & 上架
Week 11+:                                                                        ████████  Phase 4: 发布 & 迭代
```

**总预估:** 10 周到双平台发布（原方案 8 周，增加 2 周双浏览器并行成本，消除 Phase 3 适配风险）。

> **为什么总周期增加了但风险大幅降低：** 原方案 Phase 3 的「Firefox 适配 3 天」是最乐观估计，实际概率极低。双浏览器并行方案把适配成本分散到每个任务的增量 20% 中，Phase 3 不再有未知问题。代价是发布总周期从 8 周变为 10 周，但到达双平台可发布状态的确定性从「很可能延期」变为「高置信度」。

---

## 七、非功能需求

### 7.1 性能指标

| 指标 | 目标 | 测量方式 |
|------|------|---------|
| 整页翻译触发延迟 | < 500ms（首字节） | Performance API |
| 划词翻译响应 | < 800ms（含 API 调用） | 计时打点 |
| Content Script 注入 | < 50ms | Performance API |
| Popup 打开速度 | < 200ms | React Profiler |
| 内存占用（Service Worker） | < 50MB | chrome.processes |
| 缓存命中翻译 | < 10ms | 计时打点 |

### 7.2 安全要求

#### API Key 存储方案：明文 + 透明说明

（同上，不变）

#### Content Script 注入策略：ISOLATED 优先，MAIN 降级

**决策：默认 ISOLATED world，仅对已知 CSP 严格网站降级到 MAIN world。MAIN world 脚本中绝不持有 API Key。**

```
默认路径（绝大多数网站）:
  ISOLATED world Content Script
    ├── DOM 访问 ✅
    ├── chrome.runtime.sendMessage ✅ （发消息给 SW）
    ├── CSP 限制 ❌ （可能被某些网站阻止）
    └── API Key 暴露风险 ✅ （彻底隔离开）

降级路径（已知 CSP 严格网站，如 github.com）:
  MAIN world Content Script
    ├── DOM 访问 ✅
    ├── chrome.runtime.sendMessage ❌ （无法调用 chrome.* API）
    ├── CSP 限制 ✅ （绕过）
    └── API Key 暴露风险 ⚠️ （页面 JS 可读取！）
        └── 补救：MAIN world 脚本只做 window.postMessage 转发，
             ISOLATED world 代理脚本接收消息后调用 chrome.* API
```

**MAIN world 通信桥接：**

```
┌──────────────┐   window.postMessage   ┌──────────────────┐   chrome.runtime.sendMessage   ┌──────────────┐
│  MAIN world  │ ──────────────────────→ │  ISOLATED world  │ ─────────────────────────────→ │   Service    │
│  content     │ ←────────────────────── │  proxy 脚本      │ ←───────────────────────────── │   Worker     │
│  (仅 DOM+消息)│   window.postMessage   │  (chrome.* 代理)  │                               │  (持有 Key)  │
└──────────────┘                         └──────────────────┘                               └──────────────┘
```

**工程约束（不可违反）：**

| 约束 | 强制方式 |
|------|---------|
| CONTENT SCRIPT 文件中禁止 `fetch()`、`XMLHttpRequest`、任何引擎 SDK 的 import | ESLint `no-restricted-imports` + `no-restricted-globals` 规则 |
| MAIN world 文件 (`content-main.ts`) 只能 export 一个 `postMessage` 桥接函数 | 文件头注释 + PR Review Checklist |
| API Key 类型使用 `Opaque` 封装，Content Script 侧只能看到 `string & { readonly __opaque: unique symbol }`，无法误用 | TypeScript 类型隔离 |
| 引擎调用入口（`translate()`）只存在于 Service Worker 的 import 链中 | 目录结构隔离：`engines/` 目录仅被 `background.ts` import |

**代码示例：API Key 的类型隔离**

```typescript
// lib/types.ts
declare const API_KEY_BRAND: unique symbol;
export type ApiKey = string & { readonly [API_KEY_BRAND]: never };

// Service Worker — 唯一能构造 ApiKey 的地方
function getApiKey(engineId: string): ApiKey {
  return storage.get(`apikey:${engineId}`) as ApiKey;   // ← 唯一调用点
}

// engines/base.ts — translate() 接受 ApiKey 类型
abstract class AIEngine {
  abstract translate(req: TranslateRequest, key: ApiKey): Promise<TranslateResult>;
}

// Content Script — 无法获得 ApiKey 类型实例
// ❌ 编译报错：Type 'string' is not assignable to type 'ApiKey'
// ❌ 无法直接调用 fetch() 翻译 API（ESLint 阻止）
// ✅ 只能通过 sendMessage 发消息
```

### 7.3 兼容性

| 浏览器 | 最低版本 | Manifest 版本 |
|--------|---------|--------------|
| Chrome | v88+ | MV3 |
| Firefox | v109+ | MV3 |
| Edge | v88+ | MV3 |
| Safari | v15.4+ | MV3 (部分) |

---

## 八、风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|:---:|:---:|---------|
| **Google 免费接口被封** | 中 | 高 | 多引擎降级机制，用户可切换主力引擎 |
| **Manifest V3 Service Worker 生命周期限制** | 中 | 中 | 合理拆分长任务，避免 30 秒超时 |
| **DOM 遍历性能影响页面** | 低 | 高 | TreeWalker 惰性遍历，requestIdleCallback 分片 |
| **API 调用成本过高** | 中 | 中 | 缓存命中率目标 60%+，用量统计面板 |
| **Firefox MV3 兼容差异累积** | 低（已控制） | 中 | 从第 1 天起双浏览器并行开发，每周 Firefox 冒烟测试，差异实时记录到 `docs/browser-diff.md`，不再累积到 Phase 3 一次性修复 |
| **Safari 上架审核被拒** | 高 | 低 | Safari 不强制 MV3，可用兼容模式 |
| **网站 CSP 阻止 Content Script** | 低 | 中 | 默认 ISOLATED world；对于已知 CSP 严格网站（github.com 等），动态注入 MAIN world 代理脚本，通过 postMessage → ISOLATED world 桥接转发消息到 SW，API Key 始终不触及 MAIN world |

---

## 九、成功指标

| 指标 | 目标值（发布后 3 个月） |
|------|------------------------|
| Chrome Web Store 安装量 | 1,000+ |
| 评分 | 4.0+ ★ |
| 日活跃用户 | 200+ |
| 缓存命中率 | 60%+ |
| 崩溃率 | < 0.1% |
| API 调用成功率 | 99.5%+ |

---

## 十、用户获取与迁移策略

> **核心问题：** 沉浸式翻译作为市场龙头，拥有数百万用户、多年积累的信任和成熟产品。作为个人从零开始的项目，不能正面硬拼。需要精准找到它的**结构性弱点**，在它无法或不愿做好的点上建立优势。

### 10.1 沉浸式翻译的结构性弱点

| 弱点 | 具体表现 | 为什么它无法轻易补救 |
|------|---------|---------------------|
| **闭源** | 代码不透明，用户无法审计 | 商业模式决定了不能开源 |
| **中间服务器** | API 请求经过其服务器转发，数据可被记录 | 部分引擎需要服务端代理，架构使然 |
| **订阅制门槛** | Pro 会员 ¥69/年，免费版功能受限 | 这是主要盈利模式，不可能放弃 |
| **Firefox 二等公民** | 功能更新慢，体验不如 Chrome | 用户基数小，投入产出不成比例 |
| **大而全的臃肿感** | 功能越堆越多，设置界面复杂 | 已经是大用户量产品，只能做加法不能做减法 |
| **无法审计的翻译质量** | 用户不知道翻译走了哪个引擎，是否被降级 | 后端不透明 |

### 10.2 我们的差异化定位

一个十字坐标来理解市场空白：

```
            隐私透明（开源）
                │
    沉浸式翻译   │   TransFlow
    闭源收费     │   开源免费
                │
  ──────────────┼──────────────→ 技术极客/开发者
    一般用户    │
                │
    彩云小译    │   DeepL 扩展
    闭源免费     │   付费但透明
                │
```

**一句话定位：** 面向开发者和技术用户的、开源透明、API 完全本地化的多引擎翻译插件。

### 10.3 分阶段迁移策略

#### 第一阶段（0-6 月）：种子用户，不做迁移

> **关键认知：** 前 6 个月不追求用户量，追求口碑密度。不要试图说服沉浸式翻译用户迁移——他们不会来的。要找的是**沉浸式翻译也覆盖不到或覆盖不好的那批人**。

**目标人群画像：**

| 群体 | 痛点 | 获客渠道 |
|------|------|---------|
| **国内 Firefox 用户** | 沉浸式翻译在 Firefox 上体验差、更新慢 | Firefox Add-ons 商店 SEO、V2EX/Firefox 吧 |
| **后端/全栈开发者** | 介意 API Key 经过第三方服务器，想要审计代码 | GitHub Trending、掘金、Hacker News |
| **DeepSeek 生态用户** | 已购买 DeepSeek API，想要一个原生 DeepSeek 翻译工具 | DeepSeek 社区、即刻/Twitter |
| **开源爱好者** | 倾向于使用开源替代品，愿意贡献和安利 | GitHub、开源中国、RSS 订阅者 |

**具体动作：**

| 动作 | 描述 | 优先级 |
|------|------|--------|
| **GitHub 开源** | 代码完全公开，README 中英文，写好 CONTRIBUTING.md | ⭐⭐⭐ |
| **V2EX 发布帖** | 标题突出「开源 + 不经过第三方服务器 + DeepSeek」，这是 V2EX 用户最在意的三个点 | ⭐⭐⭐ |
| **掘金技术文章** | 写一篇《我写了一个浏览器翻译插件——聊聊翻译引擎的 API 设计和缓存策略》，以技术分享为主，软性推广 | ⭐⭐ |
| **即刻/少数派** | 发布使用体验，强调「翻译质量 vs 成本」的对比数据 | ⭐⭐ |
| **Firefox Add-ons** | 单独优化 Firefox 体验，反向输出口碑：「Firefox 上的沉浸式翻译替代品」 | ⭐⭐ |
| **DeepSeek 社区** | 在 DeepSeek 开发者群/论坛发布，作为 DeepSeek API 的应用案例 | ⭐⭐ |

**这一阶段的成功指标：**

- GitHub Stars > 500
- Firefox Add-ons 评分 > 4.5
- 出现自发安利（社交媒体上有人主动推荐）

#### 第二阶段（6-12 月）：功能追赶 + 制造迁移理由

> **关键认知：** 当产品功能接近沉浸式翻译 80% 时，开始制造迁移理由。用户切换工具有切换成本——需要给用户一个「不迁就亏了」的推力。

**功能追赶到什么程度：**

```
沉浸式翻译核心功能          TransFlow 状态
─────────────────────────────────────────
整页翻译      ✅            ✅ Phase 1 实现
双语对照（行间）✅            ✅ Phase 2 实现
划词翻译      ✅            ✅ Phase 2 实现
PDF 翻译      ✅ (Pro)      ⚠️ 仅免费引擎
视频字幕       ✅ (Pro)      ❌ Phase 3+
术语表         ✅ (Pro)      ✅ 免费（我们的突破口！）
自定义规则     ✅ (Pro)      ✅ 免费（突破口！）
API 自选引擎   ⚠️ 有限       ✅ 完全自由（核心优势）
```

**制造迁移推力的策略：**

| 策略 | 具体做法 |
|------|---------|
| **免费对标 Pro 功能** | 沉浸式翻译的术语表和自定义规则是 Pro 功能（¥69/年），我们完全免费。宣传口径：「你不需要为术语表付费」 |
| **隐私对比** | 画一张图：沉浸式翻译的数据流向（用户 → 沉浸式服务器 → 翻译API）vs TransFlow（用户 → 翻译API 直连）。视觉冲击力强，传播性强 |
| **成本对比** | 沉浸式翻译 Pro = ¥69/年。DeepSeek API 翻译 100 万 token ≈ ¥2。普通用户一年翻译量约 50-100 万 token，花费 ¥1-2。标题：「用我自己的 API Key 翻译一年，花了 2 块钱」 |
| **开源信任** | 定期发布安全审计报告风格的「代码透明度报告」，展示代码没后门、不偷 API Key。这在开发者圈子里传播力极强 |
| **数据主权** | 强调 API Key 存于本地 chrome.storage，翻译请求直连引擎，不经任何第三方。这是沉浸式翻译在架构上无法做到的事 |

**这一阶段的成功指标：**

- Chrome Web Store 安装量 > 3,000
- 出现迁移教程（"如何从沉浸式翻译迁移到 TransFlow"的反向安利）
- GitHub Issues 中有来自真实用户的反馈

#### 第三阶段（12 月+）：开发者生态壁垒

> **关键认知：** 长期来看，开源项目可以建一个闭源产品永远建不了的护城河——**社区贡献的引擎插件**。

沉浸式翻译的引擎是后端控制的，用户无法添加新引擎。我们作为开源插件，可以定义一个引擎插件规范，让社区贡献：

```
沉浸式翻译                        TransFlow
──────────                       ──────────
引擎由官方添加                   引擎由社区贡献
新增引擎 = 等官方排期            新增引擎 = 你写 50 行代码提交 PR
用户没参与感                     贡献者成为产品的传播者
```

**这一阶段的打法：**

1. **定义引擎插件接口** — 做一个极简的引擎开发规范，社区成员用 ~50 行代码就能接入一个新翻译 API
2. **贡献者即传播者** — 每个提交了新引擎的开发者，都会在自己的圈子里宣传「我给 XX 翻译插件写了个 XX 引擎」
3. **中文 LLM 大爆发红利** — 国内大模型厂商越来越多（通义千问、文心一言、Moonshot、智谱...），每个厂商都有 API。社区可以快速接入所有国产 LLM，沉浸式翻译做不到这么快

### 10.4 不做的事情（纪律）

| 不做 | 原因 |
|------|------|
| ❌ 做翻译后端代理服务 | 和沉浸式翻译同质化，且违背「隐私透明」定位 |
| ❌ 会员订阅制 | 开源项目不适合这个模式，可以接受捐赠但不要付费墙 |
| ❌ 在功能数量上追赶 | 沉浸式翻译几十个功能，我们做不完也不需要。聚焦 3 个核心功能做到极致 |
| ❌ 购买广告/刷量 | 个人项目预算有限，种子用户必须靠口碑获取。广告来的用户没有忠诚度 |
| ❌ 贬低竞品 | 开源社区尊重所有产品。我们的价值主张是「提供另一种选择」，不是「我们比XX好」 |

### 10.5 长期赢法

```
沉浸式翻译的护城河：用户基数、品牌认知、功能齐全
我们的护城河：      开源信任、数据主权、社区引擎生态、免费

    市场在扩大（更多中国人需要翻译）
           │
           ├── 沉浸式翻译拿走「省心用户」
           │   （不想折腾，付钱就完事）
           │
           └── TransFlow 拿走「控制欲用户」
               （在乎隐私、想自定义、喜欢开源）
               
    这个细分市场足以养活一个成功的个人开源项目。
```

---

## 十一、附录

### A. 竞品参考

| 产品 | 优势 | 劣势 |
|------|------|------|
| Google Translate 扩展 | 免费、130+ 语言 | 质量一般、无 AI 引擎 |
| DeepL 扩展 | 欧洲语言质量极高 | 仅 30 语言、需付费 |
| Mate Translate | 多引擎、UX 好 | 订阅制、不开源 |
| 沉浸式翻译 | 双语对照好 | 依赖单一后端、闭源 |

**差异化优势:** 本项目的多引擎可插拔架构 + 开源 + AI 翻译能力

### B. 技术参考链接

- WXT 框架：[https://wxt.dev/](https://wxt.dev/)
- Chrome Extension MV3：[https://developer.chrome.com/docs/extensions/mv3/](https://developer.chrome.com/docs/extensions/mv3/)
- Firefox Extension Workshop：[https://extensionworkshop.com/](https://extensionworkshop.com/)
- Anthropic API：[https://docs.anthropic.com/](https://docs.anthropic.com/)
- DeepL API：[https://www.deepl.com/docs-api](https://www.deepl.com/docs-api)
- DeepSeek API：[https://platform.deepseek.com/api-docs](https://platform.deepseek.com/api-docs)

---

## 十一、实施路线：A → B → C

> 计划书写完了。接下来不是把 Phase 1 的 12 个任务同时铺开，而是按一个精确的顺序切入，确保每走一步都有可验证的产出。

### 第一步总览

```
A: 脚手架 (30 min)
    └── npx wxt init → 空白扩展在 Chrome + Firefox 可加载
         │
         ▼
B: 接口锁死 (1-2 h)
    └── types.ts + engines/base.ts → 所有引擎实现对着接口写，不会跑偏
         │
         ├── 顺手做完 D: ESLint + CONTRIBUTING (30 min)
         │    └── no-restricted-imports/no-restricted-globals 规则写死
         │         │
         ▼         ▼
C: 缓存层 TDD (2-3 h)
    └── cache.ts + cache.test.ts → 最早能 TDD 的模块，纯函数，不依赖浏览器 API
         │
         ▼
    然后按 Phase 1 任务表继续...
```

### 为什么这个顺序

| 步骤 | 做什么 | 为什么放这里 |
|------|--------|-------------|
| **A** | `npm create wxt` 搭脚手架 | 先有可运行骨架，后续所有代码有地方验证。最坏的失败模式是搭架子都跑不起来——应该在 30 分钟内发现，而不是写完一堆代码后 |
| **B** | `types.ts` + `engines/base.ts` | 接口先行。`TextFragment`、`PageContext`、`TranslateRequest`、`TranslationEngine`、`ApiKey`（opaque）在这步全部锁死。后续引擎实现和缓存层都对着接口写，不会因为接口变来变去而返工 |
| **D** | ESLint + `CONTRIBUTING.md` | 夹在 B 和 C 之间顺手做。接口定义完了就能把 `no-restricted-imports` 写上——此时 `content.ts` 还没写，被拦截的代码一行都没有，零摩擦启用规则 |
| **C** | `cache.ts` + 单元测试 | 缓存层不依赖**任何**浏览器 API。`Map`、`hash`、`LRU` 都是纯 JS，可以在 Node 环境直接跑 Vitest。这是整个项目最早能 TDD 的模块——红的测试 → 写实现 → 绿的测试，标准 red-green-refactor 循环 |

### C 步细节：缓存层 TDD 的测试清单

```typescript
// tests/unit/cache.test.ts — 第一轮测试（纯函数，不 mock 浏览器 API）

describe('CacheKey', () => {
  it('相同原文+语言+引擎 → 相同 hash')
  it('不同原文 → 不同 hash')
  it('不同目标语言 → 不同 hash')
  it('不同引擎 → 不同 hash')
})

describe('L1Map (内存)', () => {
  it('set + get 命中')
  it('未命中返回 undefined')
  it('同 key 覆盖写入')
  it('批量 get 性能：200 条 < 1ms')
})

describe('LRU eviction', () => {
  it('超过 maxSize 时淘汰最久未使用的条目')
  it('淘汰时返回被淘汰的 key 列表（供 write-through 到 session）')
  it('get 命中刷新访问时间戳')
  it('容量守卫：bytesUsed > 80% 触发淘汰 30%')
  it('QUOTA_BYTES 兜底：淘汰 50% 后重试')
})

describe('writeThrough', () => {
  it('写入 Map 的同时异步写入 session')
  it('session 满时先淘汰再写入')
  it('session 写入失败时返回错误但不影响 Map 中的值')
})

describe('rebuild', () => {
  it('从 session 全量读取重建 Map')
  it('session 为空时 Map 为空')
  it('session 中的脏数据跳过不报错')
})
```

### 为什么缓存层可以第一个 TDD

```
依赖关系分析:
  cache.ts    → Map, hash 函数（纯 JS，无浏览器 API）
  storage.ts  → chrome.storage（需要浏览器或 mock）
  engines/*   → fetch（需要浏览器或 mock）
  dom-walker  → document.createTreeWalker（需要浏览器）
  content.ts  → 全部浏览器 API

结论: cache.ts 是依赖树的最底层纯逻辑叶子节点——最早能测试，也最早应该测试。
```

### 这一步做完的产出

```
translationPro/
├── src/
│   ├── lib/
│   │   ├── types.ts          ← 接口锁死
│   │   ├── cache.ts          ← 缓存层（TDD 完成，绿测试）
│   │   └── ...
│   ├── engines/
│   │   └── base.ts           ← 引擎接口锁死
│   └── entrypoints/
│       └── background.ts     ← 空白骨架（等待后续任务写翻译逻辑）
├── tests/unit/
│   └── cache.test.ts         ← 缓存层测试，全绿
├── eslint.config.js          ← 约束已锁
├── CONTRIBUTING.md           ← PR Checklist 已写
└── ...
```

**总耗时：约 4-5 小时（半天）**  
**里程碑：项目骨架可运行 + 接口锁死 + ESLint 规则生效 + 缓存层 TDD 完成**

---

> **下一步:** 计划书评审通过后 → A → B → C 执行
